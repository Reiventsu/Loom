#include "profile.hpp"

namespace loom {

    const sProfile sProfile::Debug          = { "debug" };
    const sProfile sProfile::RelWithDebInfo = { "relwithdebinfo" };
    const sProfile sProfile::Release        = { "release" };
    const sProfile sProfile::MinSizeRel     = { "minsizerel" };
    const sProfile sProfile::Final          = { "final" };

}
